# 金融相关报文

本文档说明了与 IIROSE 平台内的金融系统（银行、股票、支付）相关的报文。

## 银行操作

### 报文流向

- **客户端 -> 服务器**: 发起银行操作，如查询余额、存款、取款、转账。
- **服务器 -> 客户端**: 服务器返回操作结果的回调。

### 发送格式

```log
[此处填写银行操作的示例报文]
```

### 接收格式 (回调)

```log
[此处填写银行操作回调的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 股票操作

### 报文流向

- **客户端 -> 服务器**: 发起股票相关操作，如查询、买入、卖出。
- **服务器 -> 客户端**: 服务器返回股票信息或操作结果。

### 发送格式

```log
[此处填写股票操作的示例报文]
```

### 接收格式 (信息更新)

```log
[此处填写股票信息更新的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 支付/赠予

### 报文流向

- **客户端 -> 服务器**: 向另一位用户支付或赠予物品/货币。
- **服务器 -> 客户端**: 服务器返回支付操作的结果回调。

### 发送格式

```log
[此处填写支付操作的示例报文]
```

### 接收格式 (回调)

```log
[此处填写支付回调的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |