# 媒体播放相关报文

本文档说明了与房间内媒体（音乐、视频）播放控制相关的报文。

## 点播媒体

### 报文流向

- **客户端 -> 服务器**: 发送点播请求，将一个媒体（音乐或视频）添加到播放列表中。

### 发送格式 (卡片点歌)

```log
[此处填写卡片点歌的示例报文]
```

### 发送格式 (原始数据)

```log
[此处填写通过原始数据点歌的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 切歌

### 报文流向

- **客户端 -> 服务器**: 请求立即播放列表中的下一个媒体。

### 发送格式

```log
[此处填写切歌的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 移动媒体

### 报文流向

- **客户端 -> 服务器**: 在播放列表中移动一个媒体到指定位置。

### 发送格式

```log
[此处填写移动媒体的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 交换媒体

### 报文流向

- **客户端 -> 服务器**: 交换播放列表中两个媒体的位置。

### 发送格式

```log
[此处填写交换媒体的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 跳转播放进度

### 报文流向

- **客户端 -> 服务器**: 请求将当前播放的媒体跳转到指定的时间点。

### 发送格式

```log
[此处填写跳转进度的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 清空播放列表

### 报文流向

- **客户端 -> 服务器**: 请求清空整个媒体播放列表。

### 发送格式

```log
[此处填写清空播放列表的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 获取播放列表

### 报文流向

- **客户端 -> 服务器**: 请求获取当前的媒体播放列表。
- **服务器 -> 客户端**: 服务器返回播放列表数据。

### 发送格式

```log
[此处填写获取播放列表的示例报文]
```

### 接收格式 (回调)

```log
[此处填写播放列表回调的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 播放信息更新

### 报文流向

- **服务器 -> 客户端**: 当房间的播放状态（如当前播放的歌曲、播放者）发生改变时，服务器会推送此报文。

### 接收格式

```log
[此处填写播放信息更新的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |