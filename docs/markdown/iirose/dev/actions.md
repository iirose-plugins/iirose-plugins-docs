# 客户端主动动作

本文档记录了客户端可以主动向服务器发起的各种动作请求。

## 切换房间

- **目的**: 命令客户端移动到另一个房间。
- **对应编码器**: `moveRoomStart.ts`

### 发送格式

```log
[此处填写切换房间的示例报文]
```

## 获取房间用户列表

- **目的**: 请求服务器返回当前房间的完整用户列表。
- **对应编码器**: `GetUserList.ts`

### 发送格式

```log
[此处填写获取用户列表的示例报文]
```

## 获取用户资料

- **目的**: 请求指定用户的详细个人资料。
- **对应编码器**: `UserProfile.ts`

### 发送格式

```log
[此处填写获取用户资料的示例报文]
```

## 关注用户

- **目的**: 关注指定用户。
- **对应编码器**: `follow/Follow.ts`

### 发送格式

```log
[此处填写关注用户的示例报文]
```

## 取消关注用户

- **目的**: 取消关注指定用户。
- **对应编码器**: `follow/Unfollow.ts`

### 发送格式

```log
[此处填写取消关注用户的示例报文]
```

## 获取关注列表

- **目的**: 请求服务器返回自己的好友/关注列表。
- **对应编码器**: `follow/followList.ts`

### 发送格式

```log
[此处填写获取关注列表的示例报文]
```

## 点赞用户

- **目的**: 为指定用户的名片点赞。
- **对应编码器**: `like/Like.ts`

### 发送格式

```log
[此处填写点赞用户的示例报文]
```

## 取消点赞用户

- **目的**: 取消对指定用户的点赞。
- **对应编码器**: `like/Dislike.ts`

### 发送格式

```log
[此处填写取消点赞用户的示例报文]