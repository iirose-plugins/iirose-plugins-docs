# 房间管理报文 (管理员)

本文档说明了房间管理员专属的管理操作报文。这些报文通常只有在拥有相应权限时才能成功发送。

## 踢出用户

### 报文流向

- **客户端 -> 服务器**: 发送将指定用户踢出房间的请求。

### 发送格式

```log
[此处待填写踢出用户的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 禁言用户

### 报文流向

- **客户端 -> 服务器**: 发送将指定用户禁言的请求，可指定禁言时长。

### 发送格式

```log
[此处待填写禁言用户的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 黑名单管理

### 报文流向

- **客户端 -> 服务器**: 添加或移除房间黑名单中的用户。

### 发送格式

```log
[此处待填写黑名单管理的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 白名单管理

### 报文流向

- **客户端 -> 服务器**: 添加或移除房间白名单中的用户。

### 发送格式

```log
[此处待填写白名单管理的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 发布房间公告

### 报文流向

- **客户端 -> 服务器**: 发布一条新的房间公告。

### 发送格式

```log
[此处待填写发布公告的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |


## 设置房间最大人数

### 报文流向

- **客户端 -> 服务器**: 设置当前房间的最大容纳人数。

### 发送格式

```log
[此处待填写设置最大人数的示例报文]
```

### 字段解析

| 字段名 | 数据类型 | 说明 |
| :----- | :------- | :--- |
| ...    | ...      | ...  |